## 任务: 重做熵网站卡片动画（苹果级别质感）

### 范围
- 只修改 `D:\HermesAgent\projects\entropy-talk\templates\index.html`
- 只动 "你的生活里全是它" 这部分的6张卡片动画
- 不动内容结构、不动其他页面

### 当前问题
6张卡片（房间变乱/咖啡凉/耳机打结/碎鸡蛋/盐溶解/散场）点击后动画粗糙，用了 emoji + 简单位移，看起来廉价。

### 要求
- 用 SVG 代替 emoji，每个卡片做一个精致小插图
- 每张卡片点击后播放流畅的 SVG 形变动画
- 动画曲线用苹果风格的 cubic-bezier(0.2, 0.8, 0.2, 1)
- 每张卡片动画不一样，对应各自场景
- 添加微妙的光晕/渐变效果
- 卡片爆炸后自动恢复，不需要用户再点一次

### 验收标准
1. 点卡片 → 播放流畅动画（1-1.5秒）
2. 动画结束后2秒自动复原
3. 所有卡片互不干扰（点B不会打断A）
4. 风格统一：暗色背景 + 彩色光晕 + 平滑运动

### 当前代码位置
`D:\HermesAgent\projects\entropy-talk\templates\index.html`
GSAP 已通过 CDN 加载（`gsap.min.js`）
每个卡片有一个 `.anim-stage` 容器，内部 `.obj` 元素用 GSAP 驱动
